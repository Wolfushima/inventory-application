extends ../layout

block content 
    h1= title 

    ul 
        each type in cameratype_list 
            li 
                a(href=type.url) #{type.name}
                |   (#{type.category})
        
        else 
            li There are no camera types.