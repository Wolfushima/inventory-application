extends ../layout

block content 
    h1 Delete Camera Type: #{camera_type.name}

    if camera_type_cameras.length
        p #[strong Delete the following cameras before attempting to delete this camera type.]

        div(style='margin-left:20px;margin-top:20px;')

            h4 Cameras 

            dl 
            each camera in camera_type_cameras 
                dt 
                    a(href=camera.url) #{camera.name}
                dd #{camera.description}
    
    else
        p Do you really want to delete this Camera Type?

        form(method='POST' action='')
            div.form-group
                input#id.form-control(
                    type='hidden'
                    name='id'
                    required='true'
                    value=camera_type._id
                )
            button.btn.btn-primary(type='submit') Delete